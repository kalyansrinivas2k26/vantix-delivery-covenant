# n8n Workflows

No n8n workflow export is included at Gate 0.

This is intentional. The approved build sequence requires canonical governance, data and deterministic controls before workflow implementation. Creating placeholder workflow JSON would create false evidence and increase rework.

Actual importable n8n workflow JSON files begin at Gate 5 after Gate 1–4 controls are accepted.
